## Description

Use when the validation requirement is about whether the generated music semantically matches the user's text intent, genre, scene, mood, instrumentation, or other overall textual targets.

## Instructions

- Compare audio against one or more text targets using audio-text semantic similarity.
- Return a normalized score and per-text evidence.
- This is the default validator for overall query alignment.

